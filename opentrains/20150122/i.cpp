#include <bits/stdc++.h>
#define MAX 500010
using namespace std;
typedef pair<int, int> pii;

int no[MAX][MAX], hor[MAX], ver[MAX];
vector<pii> adj[MAX];

int main () {
    int n, m;
    scanf ("%d %d", &n, &m);
    for (int i = 1; i <= n+1; i++) {
        scanf ("%d", &hor[i]);
    }
    for (int j = 1; j <= m+1; j++) {
        scanf ("%d", &ver[i]);
    }

}
